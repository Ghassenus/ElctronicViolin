#pragma once
#include <lvgl.h>

/**
 * Construit la page Date & Heure dans le conteneur donné.
 * @param parent Le lv_obj_t* où construire l’UI (zone à droite du side‑menu).
 */
void ui_date_time_build(lv_obj_t* parent);