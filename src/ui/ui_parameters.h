// src/ui/ui_parameters.h
#pragma once

#include <lvgl.h>

/**
 * @brief Affiche l'écran de paramètres (date/heure et format)
 *
 * @param parent L'objet LVGL parent (écran) dans lequel construire l'UI
 */
void ui_parameters_show(lv_obj_t* parent);
